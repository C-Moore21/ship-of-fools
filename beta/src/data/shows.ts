/**
 * The beta UI now pulls shows live from `/api/*` via `hooks/useSofData`.
 * This stub stays so existing imports resolve during transition; delete
 * once no importer is left.
 */
import type { Show } from '../types/archive'
export const shows: Show[] = []
